version: 1.0

representation:
  side_panel:
    label: Identitystore User
  node:
    label:
      static: Identitystore User
      template: ${user_name}
    display_type: default

discovery:
  type: aws_identitystore_user
  dnd_supported: false
